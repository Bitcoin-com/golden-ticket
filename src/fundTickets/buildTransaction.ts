const buildTransaction = async ({
  ticketCount
}: {
  ticketCount: number;
}): Promise<string> => {
  try {
    return "";
  } catch (error) {
    return error;
  }
};

export default buildTransaction;
